apply plugin: 'java'
    
sourceSets {
    main {
        output.resourcesDir = 'build/classes/main'
    }
}

manifest.mainAttributes("Main-class" : "JavaExitSample")

task dispRes << {
    println sourceSets.main.output.resourcesDir;
}
